{
  "name": "yadra-store",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "backend/index.js", 
  "scripts": {
    "start": "node backend/index.js",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "postinstall": "echo 'Running postinstall... This will be handled by the build script.'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
